# 云端 AI 费用由本机执行月度硬上限

Jarvis 第一版对所有云端模型供应商合并执行每个本地自然月 30 元人民币的默认费用硬上限，在 15 元和 24 元时提醒，预计单次请求超过 1 元时先确认；达到上限后停止发起新的云端请求，不自动充值，也不能通过切换供应商或后台复盘绕过，只有用户明确提高上限才恢复。这个选择可能使闲聊、复盘或知识整理在当月暂时降级，换取费用可预测且不依赖供应商后台的软预算提醒；确定性监督核心和非 AI 操作入口始终继续运行。费用依据每次请求的模型、用途、token 与当时价格版本记录并定期和供应商账单核对，不为费用审计额外复制私人提示词。
