# 第一版分层试运行 DeepSeek V4

> 2026-08-15：供应商传输路径已由 [ADR 0018](0018-use-siliconflow-for-deepseek-v4.md) 取代；本文件保留当时的分层路由决策背景。

Jarvis 第一版先进行两周 DeepSeek V4 试运行：高频日常对话、提醒回复理解、承诺草稿和普通记忆候选使用 `deepseek-v4-flash` 非思考模式，低频的两周复盘、复杂原因归纳和跨资料知识综合使用 `deepseek-v4-pro` 思考模式；模型路由由本机按任务类型、资料授权和费用规则执行，模型不能自行升级。`qwen3.7-flash` 只参与同一组脱敏 Jarvis 提示词的隐私、质量、结构化可靠性、延迟和费用对照，不在故障时自动接管。这个选择利用 DeepSeek 当前的能力与成本，同时保留退出条件：首次配置必须确认退出输入训练/优化用途，价格与隐私条款需要复核，两周测试后才确认正式默认，任何供应商切换都必须由用户明确配置。
