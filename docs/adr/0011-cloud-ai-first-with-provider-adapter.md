# 第一版云端 AI 优先并保留供应商适配边界

Jarvis 第一版的自然语言理解、闲聊、复盘辅助和记忆提炼默认使用云端模型 API，本地模型作为后续针对仅限本机资料、离线对话和降低云端依赖的增强能力；确定性监督核心始终不依赖两者。业务能力从第一版通过模型供应商适配器接入，不把监督命令、记忆格式或数据库结构绑定到单一服务商。这个选择接受联网、按量费用和受控发送必要内容，换取第一版较高的模型能力与较低的本地推理集成成本；用户可以关闭云端 AI，资料源授权继续逐项生效，更换或故障切换供应商必须显式配置，不能无感转发私人内容。
