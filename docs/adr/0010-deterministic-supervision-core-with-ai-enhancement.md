# 监督采用确定性核心并由 AI 增强

Jarvis 的工作承诺、活动分类规则、监督状态转换、计时、休息、提醒升级、记录和确认规则由本机确定性监督核心执行，不依赖生成式 AI；AI 负责自然语言理解、闲聊、复盘辅助、记忆提炼和知识整理，并只能向核心提交待验证的候选操作。这个选择限制 AI 直接自治并要求为关键流程维护按钮、表单和模板等非 AI 入口，换取断网、模型故障、额度耗尽或用户关闭 AI 时监督仍可靠运行，且模型不能绕过已确认规则或追溯改写事实记录。
