# 分离本地监督核心与桌面界面进程

Jarvis 第一版由同一安装包提供两个普通用户进程：`Jarvis Core` 负责监督、计时、SQLite、外部接口和系统托盘，`Jarvis Desktop` 负责桌宠与可见界面。这个选择增加本机进程通信和状态同步工作，换取桌面动画、Web UI 或界面重启不连带终止监督；核心运行在当前用户会话中而不是高权限 Windows 系统服务，以保持前台活动观察和单人安装边界清晰。隐藏或关闭桌宠只影响界面，只有经用户确认的完全退出才同时停止核心与桌面进程。
